# easy-relationship

本仓库用于开发一款 iOS 本地化人物关系图谱应用。

## 本地运行（当前仓库状态）

本仓库目前包含可在命令行直接运行的核心模块（Swift Package），以及 iOS App 的目录骨架（需 Xcode 才能编译运行）。

### 运行单元测试

```bash
swift build --disable-sandbox
```

### iOS App（需 Xcode）

见 `ios/README.md`。
